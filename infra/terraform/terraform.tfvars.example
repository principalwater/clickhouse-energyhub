# #############################################################################
# # Пример файла с переменными.
# # Скопируйте этот файл в terraform.tfvars и заполните своими значениями.
# # ВАЖНО: Не храните файл terraform.tfvars в системе контроля версий!
# #############################################################################

# ----------------------------------
# ОБЯЗАТЕЛЬНЫЕ ПЕРЕМЕННЫЕ (ЗАМЕНИТЬ)
# ----------------------------------
# Эти пароли и ключи должны быть уникальными и надежными.

# Пароли для пользователей ClickHouse
super_user_password = "YOUR_CLICKHOUSE_PASSWORD"
bi_user_password    = "YOUR_BI_USER_PASSWORD"

# Учетные данные для MinIO (S3)
minio_root_user     = "minioadmin"
minio_root_password = "YOUR_MINIO_PASSWORD"

# Пароли для BI-инфраструктуры
pg_password         = "YOUR_POSTGRES_PASSWORD"
sa_password         = "YOUR_BI_ADMIN_PASSWORD"
superset_secret_key = "YOUR_SUPERSET_SECRET_KEY" # Сгенерируйте командой: openssl rand -base64 32

# Учетные данные для Kafka
kafka_admin_user            = "admin"
kafka_admin_password        = "YOUR_KAFKA_ADMIN_PASSWORD"
kafka_ssl_keystore_password = "YOUR_KEYSTORE_PASSWORD"

# Пользователь для SSH-доступа (требуется только для удаленного развертывания)
# remote_ssh_user     = "your_ssh_user"


# ----------------------------------
# НАСТРОЙКИ РАЗВЕРТЫВАНИЯ (МОЖНО ОСТАВИТЬ ПО УМОЛЧАНИЮ)
# ----------------------------------
# Для локального запуска рекомендуется оставить storage_type = "local_storage"
storage_type = "local_storage"

# Флаги для включения/отключения BI-инструментов
# deploy_metabase = true
# deploy_superset = true

# Kafka Configuration
kafka_version = "7.4.0"
topic_1min    = "energy_data_1min"
topic_5min    = "energy_data_5min"

# Monitoring Configuration (Portainer)
portainer_version    = "2.19.4"
portainer_https_port = 9443
portainer_agent_port = 9000

# ----------------------------------
# AIRFLOW КОНФИГУРАЦИЯ (ОПЦИОНАЛЬНО)
# ----------------------------------
# Раскомментируйте и настройте для развертывания Apache Airflow

# deploy_airflow = true

# Версии для Airflow
# airflow_version = "2.8.1"
# redis_version = "7.2-alpine"

# Порты для Airflow
# airflow_webserver_port = 8080
# airflow_flower_port = 5555

# Пароли для Airflow (ОБЯЗАТЕЛЬНО при включении)
# airflow_postgres_password = "YOUR_AIRFLOW_POSTGRES_PASSWORD"
# airflow_admin_password = "YOUR_AIRFLOW_ADMIN_PASSWORD"
# airflow_fernet_key = "YOUR_FERNET_KEY" # Сгенерируйте командой: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# airflow_webserver_secret_key = "YOUR_AIRFLOW_SECRET_KEY" # Сгенерируйте командой: openssl rand -base64 32

# Пути к директориям Airflow (можно оставить по умолчанию)
# airflow_redis_data_path = "../../volumes/airflow/redis"
# airflow_dags_path = "../../volumes/airflow/dags"
# airflow_logs_path = "../../volumes/airflow/logs"
# airflow_plugins_path = "../../volumes/airflow/plugins"
# airflow_config_path = "../../volumes/airflow/config"
