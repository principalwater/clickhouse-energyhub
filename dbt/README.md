# ClickHouse EnergyHub - dbt модуль

## Описание

Модуль dbt для ClickHouse EnergyHub с расширенными возможностями Data Quality проверок, многослойной архитектурой и оптимизацией для ClickHouse.

## Архитектура

### Слои данных

1. **RAW** - Сырые данные из Kafka
   - `raw_energy_measurements.sql` - Измерения энергии
   - `raw_devices.sql` - Данные об устройствах
   - `sources.yml` - Конфигурация источников

2. **ODS** - Operational Data Store
   - `stg_locations.sql` - Staging локаций с DQ проверками
   - `stg_devices.sql` - Staging устройств с DQ проверками
   - `stg_energy_measurements.sql` - Staging измерений энергии с DQ проверками

3. **DDS** - Data Detail Store
   - `dim_locations.sql` - Dimension таблица локаций
   - `fact_energy_consumption.sql` - Fact таблица потребления энергии

4. **Data Marts** - Готовые для BI данные
   - `daily_energy_summary.sql` - Ежедневная сводка по энергопотреблению

### Макросы

- **`dq_utils.sql`** - Утилиты для Data Quality проверок
- **`clickhouse_utils.sql`** - Специфичные для ClickHouse операции
- **`clickhouse_cluster_utils.sql`** - Макросы для работы с кластером dwh_prod
- **`incremental_utils.sql`** - Макросы для инкрементальной загрузки
- **`model_utils.sql`** - Общие утилиты для моделей

### Кластерные макросы

**Основные операции:**
- **`clickhouse_replicated_engine`** - Создание ReplicatedMergeTree с UUID
- **`clickhouse_distributed_engine`** - Создание Distributed таблиц
- **`create_replicated_table`** - Создание таблиц на кластере с ON CLUSTER dwh_prod

**Логика именования кластерных таблиц:**
- **ReplicatedMergeTree таблицы** создаются с суффиксом `_local` (например: `dim_locations_local`)
- **Distributed таблицы** имеют то же имя, что и основная таблица (например: `dim_locations`)
- **Путь для ReplicatedMergeTree**: `/clickhouse/tables/{shard}/otus_default/{table_name}_local/{uuid}`
- **База данных**: `otus_default` (создается автоматически)
- **Distributed таблицы** ссылаются на `_local` таблицы в базе `otus_default`

**Пример структуры:**
```
dim_locations_local (ReplicatedMergeTree) → dim_locations (Distributed)
fact_energy_consumption_local (ReplicatedMergeTree) → fact_energy_consumption (Distributed)
daily_energy_summary_local (ReplicatedMergeTree) → daily_energy_summary (Distributed)
```

**Операции с кластером:**
- **`clickhouse_cluster_alter_table`** - ALTER с поддержкой кластера
- **`clickhouse_cluster_drop_table`** - DROP с поддержкой кластера
- **`clickhouse_cluster_delete_from`** - DELETE с поддержкой кластера

## Установка и настройка

### Требования

- Python 3.8+
- dbt-core 1.5+
- dbt-clickhouse 1.5+

### Установка зависимостей

```bash
pip install dbt-clickhouse
```

### Настройка профиля

1. Скопируйте `profiles/profiles.yml` в `~/.dbt/`
2. Настройте параметры подключения к ClickHouse
3. Установите переменные окружения для продакшена

### Переменные окружения

```bash
export CLICKHOUSE_PASSWORD="your_password"
```

## Использование

### Запуск моделей

```bash
# Запуск всех моделей
dbt run

# Запуск конкретного слоя
dbt run --select tag:ods
dbt run --select tag:dds
dbt run --select tag:data_marts

# Запуск конкретной модели
dbt run --select stg_locations
```

### Запуск тестов

```bash
# Все тесты
dbt test

# Тесты для конкретной модели
dbt test --select stg_locations

# Кастомные тесты
dbt test --select test_type:generic
```

### Генерация документации

```bash
dbt docs generate
dbt docs serve
```

### DQ проверки

```bash
# Запуск DQ проверок
dbt run --select tag:dq_checked

# Просмотр DQ статистики
dbt run-operation get_dq_statistics --args '{table_name: "stg_locations", days_back: 7}'
```

## Data Quality

### DQ Score

Каждая модель в ODS слое имеет DQ score (0-1), который рассчитывается на основе:
- Полноты критических полей (40%)
- Валидности бизнес-правил (40%)
- Формата данных (20%)

### DQ проверки

- **Полнота данных** - Проверка на NULL значения
- **Диапазон значений** - Валидация физических ограничений
- **Бизнес-правила** - Проверка логики данных
- **Согласованность** - Проверка связей между полями

### Пороги качества

- **RAW → ODS**: DQ score ≥ 0.7
- **ODS → DDS**: DQ score ≥ 0.8
- **DDS → Data Marts**: DQ score ≥ 0.9

## Оптимизация ClickHouse

### Кластерная архитектура

- **Кластер dwh_prod** - 2 шарда × 2 реплики
- **База данных otus_default** - основная база для кластерных таблиц
- **ReplicatedMergeTree** - локальные таблицы с суффиксом `_local` и автоматической репликацией
- **Distributed** - виртуальные таблицы для распределенных операций (ссылаются на `_local` таблицы)
- **ON CLUSTER** - все операции выполняются на всех нодах кластера
- **Путь репликации**: `/clickhouse/tables/{shard}/otus_default/{table_name}_local/{uuid}`

### Индексы

- **MinMax** - Для временных полей и ID
- **Set** - Для категориальных полей
- **Bloom Filter** - Для текстовых полей

### Партиционирование

- **По дате** - Ежемесячное партиционирование
- **По региону** - Географическое партиционирование
- **По шарду** - cityHash64 для равномерного распределения

### Движки таблиц

- **ReplicatedMergeTree** - Основной движок для кластера
- **Distributed** - Для распределенных запросов
- **ReplacingMergeTree** - Для обновляемых данных
- **AggregatingMergeTree** - Для агрегированных данных

## Мониторинг

### Логирование

- DQ результаты записываются в таблицу `dq_checks.dq_test_summary`
- Автоматические алерты при превышении порогов
- Интеграция с системами мониторинга

### Метрики

- Количество пройденных/проваленных DQ проверок
- Время выполнения моделей
- Размер данных по слоям

## Разработка

### Добавление новой модели

1. Создайте SQL файл в соответствующем слое
2. Добавьте конфигурацию в `dbt_project.yml`
3. Создайте тесты в `schema.yml`
4. Добавьте документацию

### Кастомные макросы

1. Создайте файл в папке `macros/`
2. Добавьте описание в `macros.yml`
3. Протестируйте макрос

### DQ проверки

1. Определите бизнес-правила
2. Создайте DQ макросы
3. Интегрируйте в модели

## Troubleshooting

### Частые проблемы

1. **Ошибки подключения к ClickHouse**
   - Проверьте настройки профиля
   - Убедитесь в доступности сервера

2. **DQ проверки не проходят**
   - Проверьте пороги качества
   - Анализируйте данные-источники

3. **Медленная работа моделей**
   - Оптимизируйте индексы
   - Проверьте партиционирование

### Логи

- Основные логи: `logs/dbt.log`
- DQ логи: `logs/dq_checks.log`
- ClickHouse логи: системные логи ClickHouse

## 📚 Документация

- [Архитектура DWH](DWH_ARCHITECTURE.md) - Детальное описание архитектуры и слоев DWH
- [Конвенция именования кластерных таблиц](docs/CLUSTER_NAMING_CONVENTION.md) - Правила именования для кластерных таблиц
- [Примеры запросов](examples/example_queries.sql) - Готовые SQL запросы
- [Примеры кластерных таблиц](examples/cluster_table_examples.sql) - Примеры использования кластерных макросов
- [Схема тестов](models/ods/schema.yml) - Конфигурация тестов
- [Макросы](macros/) - Описание всех макросов

## 🔗 Связанные ресурсы

- [CI/CD Pipeline](../README.md#-🚀-cicd-pipeline) - Описание автоматических проверок
- [Основной проект](../README.md) - Обзор всего проекта

## 🆕 Последние обновления

### Новая логика именования кластерных таблиц

- **ReplicatedMergeTree таблицы** теперь создаются с суффиксом `_local`
- **Distributed таблицы** имеют то же имя, что и основная таблица (без `_local`)
- **База данных `otus_default`** создается автоматически для всех кластерных таблиц
- **Путь репликации**: `/clickhouse/tables/{shard}/otus_default/{table_name}_local/{uuid}`

Подробности: [Конвенция именования кластерных таблиц](docs/CLUSTER_NAMING_CONVENTION.md)

## 🚀 CI/CD Integration

Этот dbt модуль интегрирован в CI/CD pipeline GitHub Actions, который автоматически:

- ✅ **Проверяет структуру проекта** - Валидирует наличие всех слоев DWH
- ✅ **Компилирует модели** - Проверяет SQL синтаксис всех моделей
- ✅ **Валидирует слои** - Проверяет каждый слой DWH отдельно
- ✅ **Проверяет конфигурацию** - Валидирует `dbt_project.yml` и профили

**Триггеры**: Pull Request, Push to master, Manual dispatch
**Статус**: Отображается в GitHub PR с детальными отчетами

## Лицензия

MIT License - см. файл LICENSE в корне проекта.
