# Dockerfile for custom ClickHouse server image
# Can be used to add custom configs, init scripts, etc.
FROM yandex/clickhouse-server

# COPY custom_config.xml /etc/clickhouse-server/config.d/
# COPY init_db.sh /docker-entrypoint-initdb.d/
